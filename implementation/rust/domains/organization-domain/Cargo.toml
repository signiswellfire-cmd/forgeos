[package]
name = "forgeos-organization-domain"
version = "0.1.0"
edition = "2024"

[dependencies]
uuid = { version = "1", features = ["v4"], default-features = false }
getrandom = { version = "0.2", features = ["std"], default-features = false }